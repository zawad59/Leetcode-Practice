Subtract each value in the list from hashmap and then find the subtracted element in the hashmap
If exists, then return the index of the element and the current element index
Else push the element into the hashmap.